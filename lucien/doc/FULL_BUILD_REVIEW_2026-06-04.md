# Full-Build Review & Substantive Critique — 2026-06-04

Combined code-review + substantive-critique across all modules (26 review units, 2 waves), each Critical/High adversarially verified.

**Totals (post-verify):** 5 Critical, 44 High, 30 downgraded to M/L, 142 unverified Medium/Low.

## CRITICAL

- **lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/grpc/GhostCommunicationManager.java:492-499** [serialization] — Remote ghost REMOVE update is a hollow no-op but ACKed as success=true (silent divergence)
  - GhostLayerProviderImpl.removeGhostElement (lines 492-499) only logs ('Implementation would need to add removal capability to GhostLayer') and returns without removing anything. GhostExchangeServiceImpl.processStreamUpdate REMOVE case (GhostExchangeServiceImpl.java:330-336) sets success=true and the server then sends GhostAck success=true to the client (line 350). So a peer streaming a REMOVE believes the ghost was deleted, but the receiver keeps the stale element forever. This is an RDR-004-class silent data divergence: the wire reports success while the operation silently does nothing. Across a distributed run this leaves orphaned ghosts (entities that moved/were deleted on the owner rank) that are never reclaimed, corrupting neighbor/balance decisions that read the ghost layer.
  - _Fix:_ Add a real removal path to GhostLayer (e.g. removeGhostElement(entityId, treeId)) and call it; if removal is genuinely unsupported, the REMOVE case must set success=false with an explicit errorMessage so the peer does not believe the delete landed. Do not ACK success for an operation that did nothing.
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/DistributedGhostManager.java:408-431** [serialization] — createGhostForBoundaryElement transmits placeholder ghosts (empty content, (0,0,0) position, wrong ownerRank)
  - queueAndFlush (line 219) builds a ghost via createGhostForBoundaryElement and ships it to targetRank through ghostChannel.queueGhost/flushToTarget. The ghost carries placeholderContent = (Content) new byte[0], position = (0,0,0), and ownerRank = currentRank (the SENDER's rank, line 428). The comment admits 'Actual entity data will be populated by gRPC' but there is no enrichment step — the placeholder IS the payload. A receiver therefore stores a ghost with corrupt position (origin) and empty content attributed to the wrong owner. This is RDR-004-class silent data loss/corruption over the wire: real entity content and position are dropped and replaced with zeros, with no signal. The (Content) new byte[0] cast is also an unchecked cast that ClassCastExceptions if the receiver ever reads getContent() as a non-byte[] Content type.
  - _Fix:_ Populate the ghost from the local spatialIndex (the retained spatialIndex field is unused — line 60-61) with real content/position, set ownerRank to the actual owner, and serialize content via ContentSerializerRegistry rather than casting a raw byte[]. If placeholder-only is intentional for this phase, gate the flush behind an explicit 'topology-only' mode and assert no content-bearing ghost is consumed downstream.
- **sentry/src/main/java/com/hellblazer/sentry/Tetrahedron.java:orientation(...) — calls Geometry.leftOfPlaneFast** [GEOMETRY/numerical-robustness] — Inconsistent predicates: orientation uses non-robust leftOfPlaneFast while inSphere uses adaptive — Delaunay decisions can disagree
  - Tetrahedron.orientation(query,a,b,c) returns Math.signum(Geometry.leftOfPlaneFast(...)). leftOfPlaneFast is the plain floating-point determinant with NO exact/adaptive fallback (Geometry.java:2028+ documents the fast variant; the robust GeometryAdaptive.leftOfPlaneAdaptive exists and is NOT used here). Meanwhile Vertex.inSphere (Tetrahedron.inSphere -> query.inSphere) uses GeometryAdaptive.inSphereAdaptive (robust). Mixing a non-robust orientation predicate with a robust incircle predicate is the classic source of inconsistent geometric decisions: the orientation walk (locate/orientationWrt/orientationWrtCBD->orientation) may declare a point on a different side than the robust insphere flip logic assumes, producing inverted-orientation tetrahedra, non-terminating walks, or corrupted Delaunay topology near-coplanar/degenerate inputs. All of locate(), walkToTarget(), orientationWrt*, and flip orientation tests route through this fast predicate.
  - _Fix:_ Route Tetrahedron.orientation through GeometryAdaptive.leftOfPlaneAdaptive (the robust variant already present in common) so the orientation and insphere predicates are mutually consistent, OR consciously make both fast and document the loss of robustness. Add a regression test with near-coplanar inputs that currently flips between fast/adaptive sign.
- **/Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/profiler/GPUPerformanceProfiler.java:227-275, 284-331** [hollow-stub / fabricated data] — measureRealGPUBaseline / measureRealGPUOptimized fabricate GPU timings and report them as real measurements
  - Both methods are named/documented as 'measure REAL GPU baseline/optimized performance' and emit log.info("GPU baseline: ... rays in ...µs ...") / PerformanceMetrics that downstream reports and PerformanceComparison consume as truth. But no kernel is ever executed. They take startNano=System.nanoTime() (line 238/291) then compute endNano = startNano + (long)(estimatedLatency*1000) from compile-time constants (BASELINE_LATENCY_PER_100K_RAYS etc.) plus random jitter (random.nextDouble()). elapsedNano = endNano-startNano is therefore the synthetic constant, not measured time; throughput/occupancy/cacheHitRate are likewise constant+random. This is RDR-004-class deception: the success path manufactures plausible numbers while doing zero real work, and the only honest path (mock) is the catch fallback. Any consumer comparing 'baseline' vs 'optimized' is comparing two hard-coded constants, so the profiler will always 'prove' the optimization wins regardless of actual GPU behavior.
  - _Fix:_ Either (a) actually initialize DAGOpenCLRenderer, upload the DAG, run the kernel between startNano/endNano and measure the real elapsed time, or (b) rename to estimate*/simulate* and remove the 'real GPU' language, the fake System.nanoTime bracketing, and the log.info('GPU baseline...') claims so callers cannot mistake estimates for measurements. Gate any 'optimized beats baseline' assertion on real timings, not constants.
- **simulation/src/main/java/com/hellblazer/luciferase/simulation/persistence/EventRecovery.java:311-343, 319-320** [hollow-stub] — Crash recovery replays NOTHING into actual state
  - All four replay handlers (replayEntityDeparture, replayViewSynchronyAck, replayDeferredUpdate, replayMigrationCommit) only call log.debug. The bodies carry the comment 'In real implementation, would reconstruct state machine. For now, just log the replay.' recover() reads/validates/dedupes events and returns a populated RecoveredState, but ZERO state is reconstructed -- entity positions, migration commits, and deferred physics updates are all dropped on restart. The entire WAL+checkpoint durability subsystem is decorative: a crash loses all in-flight migration/physics state while the API reports a successful recovery (RDR-004 silent-data-loss class -- ACK success while dropping data).
  - _Fix:_ Either wire the replay handlers to a real state sink (apply ENTITY_DEPARTURE/MIGRATION_COMMIT to the migration state machine, DEFERRED_UPDATE to entity store) or, if recovery is genuinely deferred, make recover() fail loud / document the no-op contract so callers don't assume state is restored. Add a non-vacuous test that replays a WAL and asserts reconstructed state, not just event count.

## HIGH

- **lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/balancing/grpc/BalanceCoordinatorServer.java:198-227** [resource-leak] — BalanceCoordinatorServer streamBalanceUpdates has no concurrent-session cap (connection-level DoS)
  - streamBalanceUpdates unconditionally creates a StreamSession and puts it in activeStreams for every inbound stream. Unlike GhostExchangeServiceImpl (which enforces MAX_ACTIVE_STREAMS=1024 and returns RESOURCE_EXHAUSTED, GhostExchangeServiceImpl.java:67,154-165), the balance server accepts unbounded streams, each pinning an activeStreams entry and spawning virtual-thread tasks per onNext (line 211). A hostile/buggy peer opening many cheap streams exhausts memory/threads. The RDR-013 connection-level DoS mitigation applied to ghost was not applied to the balance coordinator server, which exposes the same StreamObserver surface.
  - _Fix:_ Apply the same MAX_ACTIVE_STREAMS guard at the top of streamBalanceUpdates, rejecting with Status.RESOURCE_EXHAUSTED once the cap is reached, exactly as GhostExchangeServiceImpl does.
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/collision/physics/ImpulseResolver.java:54-65, 81-115** [bug] — ImpulseResolver applies attractive impulse for separating contacts (no velocityAlongNormal>0 guard)
  - resolveCollision iterates every manifold point and unconditionally applies the computed normal impulse. calculateImpulseMagnitude returns j = -(1+e)*velocityAlongNormal / (invMassSum+angularEffect). When the bodies are already separating along the contact normal (velocityAlongNormal > 0), j becomes negative, so `impulse = normal*j` points into the other body and applyImpulse PULLS the two bodies together. The sibling resolver CollisionResolver.resolveCollision explicitly guards this (line 106: `if (velocityAlongNormal > 0 && penetrationDepth <= slop) return noResponse()`), but the angular ImpulseResolver — the one wired into the production shape-level path (portal CollisionDebugViewer per the class doc) — omits it. Result: sticky/attractive collisions and energy injection on glancing or resting contacts.
  - _Fix:_ Before applying, skip separating contacts: `if (velocityAlongNormal > 0) continue;` (or return j=0 from calculateImpulseMagnitude when velocityAlongNormal>0), matching CollisionResolver's guard. Recompute velocityAlongNormal per contact point (it is already per-point) so the guard is per-point.
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/collision/physics/constraints/ContactConstraint.java:99, 115** [bug] — Baumgarte stabilization bias has wrong sign — penetration correction opposes separation
  - prepare() sets `bias = -BAUMGARTE_FACTOR * penetrationError / deltaTime` (negative for any positive penetration). solve() computes `lambda = normalMass * (-vn + bias)`. The convention here (relVel = velA - velB, positive lambda applied as +impulse to A separates A from B) requires a POSITIVE bias to inject extra separating impulse proportional to penetration depth. With the negative bias, deeper penetration REDUCES the corrective normal impulse — Baumgarte stabilization fights penetration recovery instead of accelerating it, and the clamp at line 119 (`max(0, sum+lambda)`) can zero it out entirely. Penetrating stacks will sink rather than recover.
  - _Fix:_ Use `bias = BAUMGARTE_FACTOR * penetrationError / deltaTime` (positive), so `-vn + bias` adds restitution-like push-out. Verify against a single-contact resting-on-floor regression that penetration monotonically decreases over solver iterations.
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/GhostBoundaryDetector.java:451-454** [design] — createGhostElement is a hollow stub — local ghost layer is never populated
  - createGhostElement() only emits a TRACE log; it never constructs a GhostElement nor calls ghostLayer.addGhostElement(). It is the SINGLE point where the local boundary scan turns a remote-owned neighbor into a ghost (called from createGhostsForElement, line 379). A repo-wide grep confirms GhostLayer.addGhostElement() has ZERO callers in lucien-forest main source. Consequence: createGhostLayer() performs boundary detection and then always logs '... and 0 total ghosts' (getNumGhostElements() is always 0). The entire local ghost-materialization pipeline is non-functional — boundary keys are found but no ghost data is ever stored. This is silent scope reduction masquerading as a working subsystem; the javadoc frames it as a 'placeholder hook' filled by gRPC, but no such fill path exists in this module either (see DistributedGhostManager finding).
  - _Fix:_ Either implement createGhostElement to fetch/construct the ghost (key + entity data + position from the owning rank) and call ghostLayer.addGhostElement(), or fail loud (throw UnsupportedOperationException) and gate the ghost-query API so callers cannot silently observe an always-empty layer. At minimum add an invariant test asserting getNumGhostElements()>0 on a multi-rank fixture, not assertTrue(>=0).
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/fault/DefaultFailureDetector.java:176-190** [design] — Detector FAILED transition silently downgraded to SUSPECTED by FaultHandler; fabricated node UUID
  - checkHealth() transitions detectorState to FAILED and calls faultHandler.reportBarrierTimeout(partitionId). But SimpleFaultHandler.reportBarrierTimeout only escalates HEALTHY->SUSPECTED on first call (SimpleFaultHandler.java:267-271) — a partition the detector considers FAILED is only SUSPECTED in the handler until a SECOND report arrives, but the detector guards with 'if currentState != FAILED' so it never re-reports. The two state machines are permanently out of sync: a failed partition never reaches FAILED in the handler. Separately, line 187 calls reportHeartbeatFailure(partitionId, UUID.randomUUID()) fabricating a random node id, which pollutes SimpleFaultHandler.PartitionState.failedNodes with garbage UUIDs that never correspond to real nodes.
  - _Fix:_ Either have the detector call a dedicated handler API that sets terminal FAILED directly, or have the handler escalate to FAILED based on the reported severity rather than requiring two reports. Pass the actual failing node id (or a sentinel/no-arg overload) instead of UUID.randomUUID().
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/fault/CascadingRecoveryImpl.java:302-454** [test-gap] — Simulated cascading recovery is vacuous — Level 1 always succeeds, escalation logic is dead code
  - In simulated mode, attemptBarrierSync calls verifyRecovery (line 330), which calls handler.markHealthy(partitionId) then checks status==HEALTHY (lines 445-454) — this is always true because markHealthy just set it. Therefore Level 1 (BARRIER) always succeeds whenever validatePartitionState passes, so attemptStateTransfer and attemptFullRebuild are unreachable, the retry/backoff loop never runs, and the 'cascading' escalation that the class is named for is structurally dead. The simulation cannot exercise any failure/escalation path it claims to model, so any test asserting cascading behavior against this is vacuous.
  - _Fix:_ Make verifyRecovery validate something independent of the action just taken, or inject per-level success/failure outcomes (deterministic seed/strategy) so state-transfer and full-rebuild paths and the retry backoff can actually be exercised. At minimum document that simulated mode only ever exercises Level 1.
- **common/src/main/java/com/hellblazer/luciferase/common/IntArrayList.java:226-229** [Edge case / memory safety] — IntArrayList.removeRange() shares the same unvalidated-bounds bug as FloatArrayList
  - Identical defect to FloatArrayList.removeRange: only `toIndex < fromIndex` is checked. toIndex > size yields a negative-length System.arraycopy and a negative size, corrupting the list. ShortArrayList carries the same copied implementation. This is a copy-paste family bug across all three primitive list specializations.
  - _Fix:_ Add full bounds validation (fromIndex >= 0 && toIndex <= size) to IntArrayList.removeRange and ShortArrayList.removeRange to match ArrayList semantics.
- **/Users/hal.hildebrand/git/Luciferase/dyada-java/src/main/java/com/dyada/performance/MortonOptimizer.java:66-82** [CORRECTNESS / silent-data-loss] — 3D Morton decode truncates the 63-bit code to 32 bits, corrupting any coordinate above ~10 bits
  - encode3D() builds a long Morton code using interleave21() that spans up to 63 bits (3x21-bit coords). But decodeX3D/Y3D/Z3D do `compact1By2((int) morton)` (line 67/74/81), narrowing the long to int and discarding bits 32-62 BEFORE de-interleaving. So any coordinate whose bits land in the upper 32 bits of the code (i.e. component values >= ~2^10 in any axis) decode to a wrong value. The encode->decode round-trip is therefore lossy and silent: no exception, just wrong coordinates. compact1By2 is also written for `int` (final mask 0x000003ff caps output at 10 bits), so even structurally it cannot recover a 21-bit component. decodeNormalized / MortonCache.decode3DCached propagate the corruption.
  - _Fix:_ Make compact1By2 operate on `long` (use long masks covering the 0x...249 pattern across 63 bits and a final mask of 0x1FFFFF) and change decodeX3D/Y3D/Z3D to pass the full `long morton` (and `morton>>1`, `morton>>2`) without the (int) cast. Add a round-trip property test asserting decode(encode(x,y,z))==(x,y,z) for components up to 2^21-1, not just small values.
- **sentry/src/main/java/com/hellblazer/sentry/LandmarkIndex.java:94-111, 161-190 (locate/walkToTarget)** [GEOMETRY/correctness] — LandmarkIndex.walkToTarget returns null on hull-exit and uses non-robust orientation — can drop points that ARE inside the mesh
  - walkToTarget walks via current.orientationWrt(face,query) (the non-robust fast predicate) and, when getNeighbor(outsideFace) is null (walk hit a convex-hull face), sets current=null and returns null. A robust-predicate round-off that pushes the walk across a hull face yields a false 'not found'. MutableGrid.locate(Tuple3f,Random) treats a null landmark result as 'use fallback locate', so it partially self-heals, BUT MutableGrid.track(Point3f,Random) (MutableGrid.java:336-343) throws IllegalArgumentException('no located vertex') when locate returns null even though contains(p) is true-ish, and track(Vertex/Point3f,near) returns null (silently drops the point). The 10000-step cap also silently aborts. Net: valid points can be silently dropped or cause spurious exceptions under the fast predicate.
  - _Fix:_ Make walkToTarget distinguish 'hull exit / inconclusive' from 'definitively outside', and have callers always fall back to the robust full locate rather than treating null as failure. Use the same robust orientation predicate as the main locate. Replace the silent step-cap return with a fallback to deterministic locate.
- **/Users/hal.hildebrand/git/Luciferase/lucien/src/main/java/com/hellblazer/luciferase/lucien/SpatialNodeImpl.java:42, 64-71, 78-83, 173-195** [concurrency/state-corruption] — Pooled node reuse leaks stale childrenMask: default clear() never resets it
  - SpatialNodePool.acquire()/release() reset a node via node.clear() (SpatialNodePool.java:98,193). The clear() default (SpatialNodeStorage.java:43-45) only calls clearEntities(); SpatialNodeImpl does NOT override clear(), so the protected `byte childrenMask` (SpatialNodeImpl.java:42) is NEVER reset on recycle. A node that had children (childrenMask != 0) is returned to the pool, then re-acquired for a brand-new spatial key. hasChildren() (line 151) then wrongly returns true and hasChild(i) (line 139) reports phantom children. Concretely this breaks AbstractSpatialIndex.cleanupEmptyNode (line 1470: `node.isEmpty() && !hasChildren(spatialIndex)`) — the recycled node will never be reclaimed even when empty, and any split/traversal logic that consults the child bitmask reads garbage from a previous occupant. This is silent spatial-tree corruption introduced purely by enabling pooling. Additionally `childrenMask` is a non-volatile, non-atomic byte mutated with read-modify-write (mask |= / &=) under the doc claim of external write-lock protection, but cleanupEmptyNode reads hasChildren() and the pool recycle path runs without that contract held.
  - _Fix:_ Override clear() in SpatialNodeImpl to also reset childrenMask = 0 (and any subclass state, e.g. OcclusionAwareSpatialNode). Add a pool round-trip test asserting an acquired node has childrenMask==0 and !hasChildren() after a prior occupant had children.
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/migration/SpatialIndexConverter.java:235-244** [serialization-silent-data-loss] — Migration inserts null content yet counts the entity as successfully migrated
  - In migrateEntities, content is fetched via source.getEntity(entityId) (lines 226/231) and may be null (entity registered in the spatial index but no content, or a lookup that returns null). The code then unconditionally calls octree.insert(entityId, position, level, content) / tetree.insert(...) at lines 238/241 with a possibly-null content and increments processedCount (line 244). The conversion reports stats.successfulEntities for these entities while their content was silently replaced with null. This is exactly the RDR-004 class of failure: an operation ACKs success while dropping data. The only failure accounting is exception-based (catch at 245); a null-content insert throws no exception, so the loss is invisible in stats and logs.
  - _Fix:_ After fetching content, if content == null record it as a failure (failedCount + errors entry) and skip the insert, or fail the whole conversion. Do not count a null-content insert as successfulEntities. Add a regression test that seeds an entity whose getEntity() returns null and asserts stats.failedEntities == 1 (not successfulEntities).
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/migration/SpatialIndexConverter.java:264-303** [serialization-silent-data-loss] — Spanning entities collapsed to single deepest-level insert during conversion
  - findEntityLevel collects ALL nodes whose entityIds() contain the entity (lines 267-269 / 286-288) — a spanning entity legitimately lives in multiple nodes — then returns only the single deepest level via max() (lines 273-276 / 295-298). migrateEntities then performs exactly ONE insert at that level (lines 238/241). Any entity that spanned multiple nodes/levels in the source loses its spanning placement after conversion; it is silently down-converted to a single point placement. processedCount still counts it as a full success. The 'Default to a reasonable level' fallback (lines 280/302) of min(3,maxDepth) is also an arbitrary placement that does not round-trip the source level for entities not found in any node.
  - _Fix:_ Detect spanning entities (nodes.size() > 1 or presence of EntityBounds) and migrate using the spanning insert path / preserve all levels, or at minimum record them as a distinct 'lossy' stat category rather than successfulEntities. Round-trip the actual stored level instead of the min(3,maxDepth) guess for not-found entities (treat not-found as a failure).
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/entity/EntityManager.java:139** [clock-injection] — createOrUpdate mixes frame number and System.currentTimeMillis() into one timestamp, and violates clock mandate
  - long timestamp = frameManager != null ? frameManager.getCurrentFrame() : System.currentTimeMillis(); then dynamics.updatePosition(position, timestamp). This is a direct System.currentTimeMillis() call in production code (clock-injection mandate violation — should use an injected Clock). Worse, the two branches produce semantically incompatible units: getCurrentFrame() is a monotonic frame counter (small integers), System.currentTimeMillis() is epoch milliseconds (~1.7e12). EntityDynamics.updatePosition computes velocity from delta-timestamp; depending on which branch ran historically, the same dynamics object can see a frame-count delta of 1 and an epoch-ms delta of thousands, producing wildly wrong velocities. Same defect at line 326.
  - _Fix:_ Inject a Clock (Clock.system() default, setClock() for tests) and use clock.currentTimeMillis() for the no-frame-manager branch. More importantly, do not interchange frame numbers and wall-clock ms in the same field consumed by velocity math — pick one time base for dynamics (frame-based, or clock-based) and use it consistently across both branches.
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/tetree/TetreeLevelCache.java:165-170, 364-375, 130-136, 320-331, 148-153, 99-115** [CONCURRENCY] — Publication-order race on all TetreeLevelCache static caches (key written before value, no memory barrier)
  - Every cache-write method writes the parallel KEYS[slot] array first, then the VALUES[slot] array, with no synchronization, no volatile, and no atomic publication (e.g. cacheTetreeKey L168-169 writes TETREE_KEY_CACHE_KEYS[slot] then TETREE_KEY_CACHE_VALUES[slot]; same pattern in cacheParentChain L134-135, cacheParent, cacheParentType, cacheIndex). The readers (getCachedTetreeKey L368-370, getCachedParentChain L324-326, etc.) compare only KEYS[slot]==key then return VALUES[slot]. Under concurrent access (these are static, shared process-wide, and tmIndex()/parent-chain walks are hot paths reached from the concurrent AbstractSpatialIndex), a reader thread can observe the freshly-published key but a stale or null value for that slot due to lack of a happens-before edge across the two non-volatile arrays. Result: getCachedParentChain/getCachedTetreeKey returns null (→ recompute, benign) OR a value belonging to a DIFFERENT (x,y,z,l,type) tuple that previously occupied the slot → wrong TetreeKey / wrong parent chain → wrong SFC keys and silent spatial-index corruption. Java arrays of references give no per-element visibility guarantees without a barrier.
  - _Fix:_ Publish key+value atomically. Either (a) replace the parallel-array direct-mapped caches with a single ConcurrentHashMap<Long, V> (matches TetreeRegionCache which already does this), or (b) store a single immutable {key,value} holder per slot in a volatile/AtomicReferenceArray so the key check and value read see a consistent pair. At minimum make the VALUES arrays AtomicReferenceArray and write value-before-key with the key being the volatile publish point, and have the reader re-read the value after confirming the key (double-check).
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/tetree/TetreeNeighborFinder.java:392-413** [HOLLOW STUBS] — findEdgeNeighborsAtLevel / findVertexNeighborsAtLevel / findVertexNeighborsAtFinerLevels are hollow stubs returning empty lists
  - All three private helpers allocate an empty ArrayList and return it with a 'This is a placeholder for the complex geometric calculation' comment (L395, L403, L411) — no traversal is performed. They are reached from the PUBLIC API: findEdgeNeighbors (L88) calls findEdgeNeighborsAtLevel for both the parent level (L127) and child level (L133); findVertexNeighbors (L274) calls findVertexNeighborsAtLevel (L327) and findVertexNeighborsAtFinerLevels (L334). Consequently edge/vertex neighbor queries that should include neighbors at a coarser or finer refinement level silently return an incomplete set (an ACK-success-with-dropped-data pattern). A caller cannot distinguish 'genuinely no cross-level neighbor' from 'not implemented', so downstream balancing/ghost/collision logic that relies on complete neighbor sets gets wrong topology with no error.
  - _Fix:_ Either implement the cross-level edge/vertex traversal (DFS over children sharing the edge/vertex, validated by reciprocity per the project's involution rule), or, if cross-level neighbors are out of scope for this phase, make the public findEdgeNeighbors/findVertexNeighbors throw UnsupportedOperationException for the cross-level branch (fail-loud) rather than silently returning a partial list. Add a non-vacuous test asserting the exact expected neighbor count, not assertTrue(count>=0).
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/neighbor/MortonNeighborDetector.java:139-185** [correctness/geometry] — Morton encode/decode asymmetry produces wrong neighbor keys
  - decodeCoordinates() decodes the morton code and then shifts coordinates UP by (maxLevel - level) into the level-21 coordinate space (lines 166-177). findNeighborsWithOffsets() adds cellSize offsets in that same level-21 space (145-147), then calls encodeMorton() which feeds those level-21-space coordinates straight into MortonCurve.encode((int)x,...) with NO compensating down-shift (182-185). The resulting morton code is encoded from level-21 grid units but the key is constructed with element.getLevel() (155). So the embedded code and the level are inconsistent and the neighbor key does not correspond to the geometric neighbor cell. Note isBoundaryElement() (101) uses MortonCurve.decode WITHOUT the up-shift, contradicting decodeCoordinates and confirming the two code paths disagree on the coordinate convention.
  - _Fix:_ Pick one coordinate convention. Either keep grid-unit coords throughout (decode without up-shift, add +/-1 offsets in grid units, encode directly) or work in level-21 space and down-shift by (maxLevel-level) before MortonCurve.encode. Add a round-trip test: for a known cell, findFaceNeighbors then decode each result and assert coordinates differ by exactly one cellSize on one axis.
- **lucien/src/main/java/com/hellblazer/luciferase/lucien/occlusion/VisibilityStateManager.java:125-176** [concurrency] — updateVisibility is a non-atomic read-modify-write on ConcurrentHashMap state
  - The class advertises thread safety via concurrent collections (line 42), but updateVisibility() does get (126) -> compute new state (131-162) -> updateStateInfo put (166) with no per-entity locking or CAS. Two concurrent calls for the same entity (e.g. a render-thread visibility update and createTBV's internal updateVisibility at line 210) interleave: both read the same currentState, both compute and write, and one transition is silently lost, including lost TBV removal/expiry accounting. Additionally createTBV stores the TBV (206) and only then calls updateVisibility(false) (210) in a separate step, so an observer can see state=HIDDEN_WITH_TBV with the TBV not yet present, or VISIBLE->HIDDEN_WITH_TBV set by the plain state machine (138) with no TBV ever created, after which the expiry branch (148-149) sees tbv==null and the entity is stuck in HIDDEN_WITH_TBV forever.
  - _Fix:_ Use entityStates.compute(entityId, ...) so the read-modify-write is atomic per key, and drive TBV side effects inside that lambda or via a single guarded path. Add a concurrent test hammering updateVisibility on one entity from N threads and asserting transition count == observed transitions.
- **/Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/dag/DAGBuilder.java:236, 247-257, 287-298** [SERIALIZATION_SILENT_DATA_LOSS / dedup correctness] — DAG deduplication keys on a 64-bit-truncated SHA-256 digest with NO collision verification
  - computeSubtreeHashes() stores nodeHashes[nodeIdx] = hasher.digest(), and JavaMessageDigestHasher.digest() (JavaMessageDigestHasher.java:55-60) truncates the 256-bit SHA-256 to the first 8 bytes as a long. identifyDuplicates() then treats equal 64-bit hashes as identical subtrees (hashToCanonical.putIfAbsent(hash, nodeIdx)), and buildCompactedDAG() collapses every node sharing a hash into the canonical node (oldToNew[nodeIdx]=oldToNew[canonical]) WITHOUT ever comparing the actual subtree structure/descriptors. A single 64-bit collision between two structurally-different subtrees silently merges them, corrupting the DAG (wrong geometry, wrong child pointers) while build() reports success and a bogus compression ratio. This is exactly the RDR-004 class: ACK success while dropping/replacing data. By the birthday bound a 64-bit space starts colliding around ~5 billion subtrees, but adversarial or hash-weak input can collide far sooner, and there is no detection at all.
  - _Fix:_ After hash bucketing, verify candidate duplicates by deep structural equality (compare child/contour descriptors and recursively the mapped child sets) before collapsing; OR key the map on the full byte[] digest, not the truncated long. Add a regression test that injects two distinct subtrees with a forced hash collision and asserts they are NOT merged.
- **/Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/io/ESVOSerializer.java:150-182 (writeNodes), 45-74 (serialize)** [SERIALIZATION_SILENT_DATA_LOSS] — Serializer/deserializer drop far-pointer array on round-trip; child resolution corrupts silently
  - writeNodes() persists ONLY node.getChildDescriptor() and getContourDescriptor() (plus 4 zero padding bytes) per node. ESVOOctreeData carries a separate farPointers[] array (ESVOOctreeData.java:186, setFarPointers) which is the indirection table used whenever a node has isFar()=true: childPtr then indexes farPointers[] to get the real relative offset. Neither ESVOSerializer nor ESVODeserializer.readNodes (ESVODeserializer.java:127-149) writes/reads farPointers. After a round trip getFarPointers() is empty, so any node with the far bit set resolves its children against an empty table -> wrong/out-of-bounds child indices. The file format also has no field for it, so the loss is permanent and silent (serialize() logs success). Same class as the DAG bug: success ACK while data is dropped.
  - _Fix:_ Extend the file format to persist farPointers[] (length + entries) and restore it in the deserializer; round-trip getCoordinateSpace too. Add a round-trip test using an octree that actually has isFar()=true nodes and assert getFarPointers() and child resolution are byte-identical post-load.
- **/Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/io/ESVOSerializer.java:54, 140-148 (countNodes) and ESVODeserializer.java:41,131** [SERIALIZATION / resource] — countNodes returns maxIndex+1, materializing a dense file for sparse octrees (memory blowup + silent zero-fill)
  - countNodes() returns indices[last]+1 (max index + 1), not the actual stored count. For a sparse ConcurrentSkipListMap-backed octree with a few high-index nodes, writeNodes loops 0..maxIndex writing a zero (all-empty) node for every gap (lines 163-176), and allocates a single nodeBuffer of nodeCount*12 bytes up front (line 159) -- a sparse octree with one node at index 1,000,000 allocates ~12 MB and writes a million empty nodes. Deserializer mirrors this (capacity = max(nodeCount*8+1024,100000), allocate nodeCount*12). This is a DoS/OOM amplification on a crafted/large-index file and conflates 'absent node' with 'empty node' on reload.
  - _Fix:_ Serialize the actual (index,node) pairs (sparse format) or stream nodes in chunks rather than one giant buffer; cap/validate nodeCount against file length before allocating ByteBuffer.allocate(nodeCount*12).
- **/Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/DAGOpenCLRenderer.java:619-650, 587-613, 522-529** [hollow-stub / silent scope reduction] — Tile-based dispatch executes nothing — KernelExecutor body is empty TODOs returning dummy results
  - executeKernel() routes to executeTileBasedDispatch() whenever useTileBasedDispatch && tileDispatcher!=null (enabled live via -DENABLE_TILE_DISPATCH, line 111-115). That path calls tileDispatcher.dispatchFrame(...createKernelExecutor()). But createKernelExecutor() (619-650) is entirely stubbed: executeBatch() falls back to executeSingleRay(); executeSingleRay() has an empty try block with only comments ('Upload subset / Execute kernel / Download results'); getResult() returns a hard-coded dummy RayResult(0,0,0,-1) marked TODO. So with the tile flag on, the renderer silently produces an all-miss frame while logging success metrics (line 611). No fail-loud guard prevents the flag from selecting this dead path.
  - _Fix:_ Implement the executor (upload ray subset, dispatch kernel, read results) or make initializeTileDispatcher()/executeKernel() throw UnsupportedOperationException when the tile path is selected but the executor is unimplemented, so the dead path cannot silently zero a frame in production.
- **/Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/OctreeGPUMemory.java:137-150, 158-173, 218-241** [concurrency / use-after-free] — writeNode/readNode do a TOCTOU check on 'disposed' then touch nodeBuffer that dispose() concurrently frees+nulls
  - dispose() is synchronized and does memAlignedFree(nodeBuffer); nodeBuffer=null (234-237). writeNode/readNode/getNodeBuffer are NOT synchronized: they check 'if(disposed)' then call nodeBuffer.putInt(...) (148) / nodeBuffer.getInt(...) (169). A dispose() interleaving after the disposed-check but before the buffer access yields either an NPE (nodeBuffer==null) or, worse, a write/read against freed native memory (memAlignedFree already ran but disposed not yet set true). The class comment claims volatile 'disposed' makes staging-thread reads safe, but the actual data accessors are unguarded, so the volatile only protects the SSBO-upload guard, not writeNode/readNode. Classic check-then-act use-after-free on native memory.
  - _Fix:_ Guard writeNode/readNode/getNodeBuffer/uploadToGPU under the same lock as dispose() (or use a ReadWriteLock: shared for accessors, exclusive for dispose), so the disposed-check and the buffer touch are atomic. Set disposed=true BEFORE freeing the buffer under the lock.
- **/Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/GPUBufferPool.java:46-59, 137-183, 191-213** [hollow-abstraction / wrong abstraction] — GPUBufferPool pools no GPU memory — PooledBuffer is pure byte-accounting with no cl_mem/MemorySegment handle
  - The class is documented as a 'Buffer pool for GPU memory allocation' that reduces 'allocation overhead and fragmentation'. But PooledBuffer (46-59) carries only id, sizeBytes, sizeClass, timestamps — no cl_mem long, no ByteBuffer, no MemorySegment. allocate()/release() only mutate AtomicLong counters and a free-list of metadata records; no actual GPU buffer is ever created, reused, or freed. 'Reuse from free pool' (149-160) discards the reused record entirely and mints a fresh UUID, so even the metadata isn't reused — the hit just avoids incrementing missCount. Callers believing they obtained a reusable GPU buffer get nothing usable; the eviction/LRU logic operates on phantom bytes. This is an abstraction that simulates pooling without pooling.
  - _Fix:_ Either store and return a real handle (cl_mem from clCreateBuffer or an FFM MemorySegment) in PooledBuffer and free it on eviction (clReleaseMemObject), or downgrade the class to an explicitly-named accounting/quota tracker and remove the 'reduce allocation overhead/fragmentation' claims so callers don't assume real buffer reuse.
- **render/src/main/java/com/hellblazer/luciferase/esvt/traversal/ESVTTraversal.java:206-215** [GEOMETRY / CPU-GPU divergence] — CPU applies BEY_NUMBER_TO_INDEX to childOrder values; GPU shader uses them directly — index-convention mismatch
  - ESVTChildOrder.getChildOrder() returns values sourced from TetreeConnectivity.CHILDREN_AT_FACE, which the CPU traversal treats as Bey indices and converts to Morton via BEY_NUMBER_TO_INDEX[parentType][beyIdx] (line 215). The code's own comment (lines 203-205) admits the GPU shader's CHILD_ORDER literal consumes these same values DIRECTLY without the conversion, calling this 'a pre-existing inconsistency under review in Luciferase-waft'. If the CPU and GPU disagree on whether the table values are Bey or Morton indices, the two renderers descend into different children for the same ray, producing divergent images / wrong hits on GPU. This is exactly the centroid/childOrder index-space consistency hazard called out for this unit. The ESVTChildOrderShaderParityTest pins the byte values of the table but does NOT prove both sides interpret those bytes in the same index space.
  - _Fix:_ Decide one canonical index space at the table boundary. Either (a) make ESVTChildOrder emit Morton indices (apply BEY_NUMBER_TO_INDEX inside computeChildOrder) and have BOTH CPU and GPU consume directly, or (b) keep Bey and apply the conversion in the shader too. Then add a parity test that exercises a real ray through both CPU and GPU paths and asserts identical hit node, not just identical table bytes. Resolve Luciferase-waft before relying on GPU output.
- **render/src/main/java/com/hellblazer/luciferase/esvt/core/ESVTData.java:275-292** [SERIALIZATION SILENT DATA LOSS] — ESVTData.fromByteBuffers/fromByteBuffer silently drop far pointers
  - fromByteBuffers() reconstructs ESVTData via the 6-arg constructor new ESVTData(nodes, contours, rootType, maxDepth, leafCount, internalCount) (line 291), which routes to the constructor that sets farPointers = new int[0]. There is no farPointers parameter or read path at all. Any tree large enough to need far pointers (the builder emits them for relativeOffset > 32767, ESVTBuilder line 763-784) that is round-tripped through these in-memory buffer helpers loses ALL far pointers. resolveChildPtr() then returns the raw 15-bit far INDEX as if it were a node offset (ESVTData line 177-183), silently producing corrupt child links / wrong geometry with no error. Same RDR-004 class: a success-returning converter that drops a whole data channel.
  - _Fix:_ Either delete the lossy fromByteBuffers/fromByteBuffer helpers (file ESVTDeserializer already round-trips far pointers correctly), or extend them to accept and read a farPointers buffer and pass it to the 7-arg constructor. Add a round-trip test with a tree that actually contains far pointers and assert farPointerCount() is preserved.
- **render/src/main/java/com/hellblazer/luciferase/render/benchmark/NodeReductionComparator.java:111-128** [vacuous-benchmark / silent-scope-reduction] — Node-reduction benchmark never builds per-tile BeamTrees — the headline metric is fabricated
  - compare() builds a REAL global BeamTree (globalNodes = globalTree.getStatistics().totalBeams()) but NEVER builds the per-tile BeamTrees it claims to compare against. In the high-coherence branch it sets tiledNodes = globalNodes (forcing reductionRatio = 0). In the low-coherence branch it sets tiledNodes = totalTiles (a raw tile COUNT — '1 virtual node per tile'), then computes reductionRatio = 1 - (totalTiles / globalNodes). The reported 'node reduction' is therefore an arithmetic artifact of comparing an actual tree-node count against a constant tile count, not a measurement of any tiled rendering approach. The 30% reduction target (Phase5a5BenchmarkRunner.targetMet()) is validated entirely against this non-measurement. This is the RDR-class 'looks implemented but measures nothing' pattern: the doc comment even calls tiledNodes 'a conservative estimate'. Any claim derived from this benchmark (node reduction %, target-met) is unsubstantiated.
  - _Fix:_ Actually build BeamTrees per tile (partitionRaysIntoTiles already produces the ray groups) and sum their getStatistics().totalBeams() into tiledNodes, so reductionRatio reflects real per-tile vs global node counts. Until then, do not report reductionRatio/targetMet as a result.
- **render/src/main/java/com/hellblazer/luciferase/sparse/io/SparseVoxelIOUtils.java:97-101** [correctness / partial-read] — readIntArray treats a single FileChannel.read as complete — spurious IOException on valid large files
  - readIntArray allocates count*4 bytes, does ONE channel.read(buffer), and throws IOException if bytesRead != count*4. FileChannel.read is permitted to return fewer bytes than the buffer's remaining capacity even when more data is available (large reads, blocking channels, sockets-as-channels). For large voxel int arrays this will throw 'Expected N bytes, read M' on perfectly valid data, corrupting load of any sufficiently large sparse-voxel file. Separately, count*4 is an int multiply: count > ~536M overflows to negative/wrong, making the length check and allocation wrong (DoS / silent truncation).
  - _Fix:_ Loop until the buffer is full (while (buffer.hasRemaining()) { int n = channel.read(buffer); if (n < 0) throw EOF; }) instead of a single read. Compute the byte count as a long (long bytes = (long) count * 4) for the comparison and guard against overflow before allocate.
- **/Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/von/transport/VonTransportFilter.java:47-59** [security] — Deserialization filter restricts classes but sets no resource limits (maxbytes/maxarray/maxdepth/maxrefs) -> OOM DoS amplification
  - VonTransportFilter.PATTERN is a pure class allow-list ending in '!*'. It allows java.util.ArrayList and java.lang.* (String). ObjectInputFilter resource-limit directives (maxbytes=, maxarray=, maxdepth=, maxrefs=) are entirely absent, and SocketServer.handleClient (SocketServer.java:143-146) reads readObject() off an untrusted network socket with no SO_TIMEOUT and no length prefix. A malicious or buggy peer can send a single serialized TransportVonMessage carrying a gigantic ArrayList of Strings (all allow-listed types) or a deeply nested/back-referenced graph and exhaust heap before the (TransportVonMessage) cast. This is the Java-serialization analogue of a missing gRPC maxInboundMessageSize: the class allow-list closes the RCE gadget vector (RDR-004) but leaves the resource-exhaustion vector wide open. Loopback-only binding (SocketServer.java:102) is the only mitigation, and it is explicitly slated to be lifted (Inc 7+).
  - _Fix:_ Append resource caps to PATTERN, e.g. 'maxbytes=<N>;maxarray=<N>;maxdepth=<N>;maxrefs=<N>;' (sized to the largest legitimate GhostSync/JoinResponse payload), and set clientSocket.setSoTimeout(...) in SocketServer.handleClient so a slow-loris / stalled read cannot pin a thread+socket forever.
- **simulation/src/main/java/com/hellblazer/luciferase/simulation/topology/TopologyExecutor.java:248-262, 323-332, 395-426** [serialization-silent-data-loss] — Split rollback orphans entities: grid bubble removed but accountant entity moves never reversed
  - On a SplitProposal, BubbleSplitter.execute() relocates entities to the new bubble via accountant.moveBetweenBubbles() (BubbleSplitter.java:265) and adds the bubble to the grid (recordBubbleAdded). If a later executor-level check fails (accountant.validate() at TopologyExecutor.java:325 or the conservation check at :335), rollback() is invoked. rollback() only undoes GridOperations: BubbleAdded.undo (GridOperation.java:75-81) calls grid.removeBubble(newBubbleId). It does NOT reverse the accountant entity moves (explicitly documented limitation, TopologyExecutor.java:387-391 / 424-425). Result: entities remain assigned to newBubbleId in the EntityAccountant while the grid no longer has that bubble — they are spatially unreachable (getBubbleById returns null) yet still counted by getTotalEntityCount()/getDistribution(). getTotalEntityCount() therefore still equals totalBefore, so the post-rollback state silently 'passes' numerically while entities are orphaned. The reassuring log line 'EntityAccountant ensures entity tracking consistency - no entities lost' is misleading: tracking is consistent, spatial reachability is not. This is an RDR-004-class success-while-losing-data defect.
  - _Fix:_ Make rollback reverse the accountant entity movements, not just grid structure. Capture the per-entity source/target bubble assignments performed during the operation and replay them in reverse (accountant.moveBetweenBubbles back to source) before/after removing the bubble, OR keep the bubble in the grid until accountant state is restored. At minimum, after a split rollback, move every entity assigned to newBubbleId back to sourceBubbleId in the accountant so no entity references a removed bubble. Add a test that fails the executor-level validate() after a successful splitter run and asserts every entity resolves to a live grid bubble.
- **simulation/src/main/java/com/hellblazer/luciferase/simulation/topology/events/TopologyEventStream.java:162-180** [serialization-silent-data-loss] — Locale-dependent String.format(%f) emits malformed JSON for centroids/densityRatio in non-US locales
  - moveEventToJson (lines 162-171) and densityStateChangeEventToJson (lines 173-180) build JSON via String.format(...) without a Locale argument, using %f for centroid coordinates and densityRatio. String.format uses the default locale; in locales that use comma as the decimal separator (e.g. de_DE, fr_FR) %f produces e.g. 1,5 instead of 1.5, yielding invalid JSON like {"x":1,500000,...}. WebSocket clients receive corrupt payloads (parse error or shifted fields) — silent data corruption on the visualization stream dependent on JVM default locale.
  - _Fix:_ Pass Locale.ROOT (or Locale.US) as the first argument to every String.format that emits numeric JSON, e.g. String.format(Locale.ROOT, ...). Apply to all five *ToJson methods for consistency.
- **/Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/bubble/TetrahedralMigration.java:228, 262, 141** [CORRECTNESS] — Migration failure metric double-counted (executeMigration records failure, caller records it again)
  - executeMigration() calls metrics.recordFailedMigration() internally on the null-bubble path (line 228) and on the rollback path (line 262) before returning false. The caller checkMigrations() then ALSO calls metrics.recordFailedMigration() because executeMigration returned false (line 141). So failureCount is incremented twice for every src/dst-null and rollback-failure migration. getFailureCount() (TetrahedralMigrationMetrics.getFailureCount) therefore over-reports failures, corrupting any test or operator dashboard keyed on it and masking the true failure rate.
  - _Fix:_ Pick one owner of the failure metric. Remove the recordFailedMigration() calls inside executeMigration (lines 228, 262, 272) and let checkMigrations own all failure recording via its return-value branch; or vice versa. Add a unit test asserting failureCount == exact expected N (not >0) after a forced null-route and a forced rollback.
- **/Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/bubble/MultiBubbleSimulation.java:372-375** [RESOURCE LEAK] — close() only closes executionEngine — leaks every bubble's RealTimeController and AutoCloseable BubbleGhostCoordinator
  - MultiBubbleSimulation creates EnhancedBubbles (via gridOrchestrator/createBubbles), each of which owns a RealTimeController (a thread-bearing resource) and a BubbleGhostCoordinator that implements AutoCloseable and registers a tick listener on its RealTimeController. close() only calls executionEngine.close(); it never iterates getAllBubbles() to close their ghost coordinators or stop their controllers. Every simulation instance leaks N ghost-coordinator tick-listener registrations and N controllers. In a test suite or long-running host that creates many simulations this accumulates threads/listeners (precisely the leak class Luciferase-zwyf2/0frcy.57 were opened for, left unclosed at the orchestrator level).
  - _Fix:_ In close(), after executionEngine.close(), iterate getAllBubbles() and close each bubble's resources (requires EnhancedBubble to expose close() — see related finding). Add a leak-regression test asserting RealTimeController.getTickListenerCount() returns to baseline and no non-daemon threads remain after close().
- **/Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/bubble/EnhancedBubble.java:42, 111-116, 189** [RESOURCE LEAK] — EnhancedBubble owns AutoCloseable ghost coordinator + RealTimeController but is not AutoCloseable and exposes no close()
  - EnhancedBubble constructs a BubbleGhostCoordinator (line 111) which is AutoCloseable and registers a tick listener on the RealTimeController (BubbleGhostCoordinator lines 67-72), and in the 3-arg constructor it also creates the RealTimeController itself (line 61). EnhancedBubble does not implement AutoCloseable and provides no method to close/deregister the coordinator or stop the controller. The coordinator's close() (which deregisters the listener per Luciferase-zwyf2) is therefore never reachable through normal bubble dissolution. Any caller that drops an EnhancedBubble leaks the tick-listener registration (keeps the coordinator alive, keeps firing) and, if the controller was started, the tick thread.
  - _Fix:_ Make EnhancedBubble implement AutoCloseable; in close() call ghostCoordinator.close() and, when the bubble created the controller (3-arg ctor), realTimeController.stop(). Track ownership of the controller so an injected controller is not stopped by the bubble.
- **simulation/src/main/java/com/hellblazer/luciferase/simulation/animation/VolumeAnimator.java:148, 55, 59** [clock-injection] — AnimationFrame.lastActive captures Clock.system() at field-init, before setClock() can run
  - The outer field `frame = new AnimationFrame(100)` (line 59) is constructed during VolumeAnimator's field-init phase. The inner field `lastActive = clock.nanoTime()` (line 148) is therefore evaluated while `clock` is still the default `Clock.system()` (line 55). Any test or caller that injects a deterministic clock via setClock() (line 64) AFTER construction will have its first `cumulativeDelay += start - lastActive` (line 173) computed against a real wall-clock nanoTime baseline, mixing system time with injected time. This is the documented field-init-captures-system-clock bug class. The frame is also created eagerly in the constructor so there is no window to set the clock first.
  - _Fix:_ Do not initialize lastActive at field-init. Initialize it lazily on the first track() call (e.g. `if (frameCount == 0) lastActive = clock.nanoTime();`), or defer AnimationFrame construction until start()/after the clock is settable, or pass the Clock into AnimationFrame's constructor so it reads the injected clock.
- **simulation/src/main/java/com/hellblazer/luciferase/simulation/animation/VolumeAnimator.java:73-77** [resource-leak] — VolumeAnimator owns a RealTimeController but is not AutoCloseable; no shutdown path
  - The constructor creates `new RealTimeController(name)` (line 74) and registers it globally via `Kairos.setController(controller)` (line 76). VolumeAnimator exposes start() (line 93) but no stop()/close(), and does not implement AutoCloseable. The RealTimeController (which owns scheduling threads) is never shut down, leaking the controller thread for every VolumeAnimator created. Tests that create many animators will accumulate live controllers.
  - _Fix:_ Implement AutoCloseable; add close()/stop() that calls controller shutdown. Avoid the global Kairos.setController side effect in the constructor, or document and reverse it on close.
- **/Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/distributed/migration/CrossProcessMigration.java:431-450** [concurrency] — Orphan cleanup removes the active entity but never completes its CompletableFuture -> caller hangs forever
  - cleanupOrphanedEntities() removes a stuck entity from activeEntities (iterator.remove(), line 447) and records a failure metric, but it never completes the entity's CompletableFuture<MigrationResult>. The migrate() caller holds that future (returned at line 302) and a genuinely-orphaned migration leaves that future unresolved permanently, so any caller doing future.get() blocks indefinitely while the system has already given up on the migration. Additionally entity.phaseStartTime (read at line 443) is mutated by the PrimeMover state-machine thread and read here without synchronization (visibility/torn-read risk for the age comparison).
  - _Fix:_ On orphan removal, complete the future exceptionally (or with a MigrationResult failure 'ORPHANED_CLEANUP') so awaiting callers unblock. Make phaseStartTime volatile (or read it under the entity lock) to guarantee visibility of the age used for the timeout decision.
- **/Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/viz/render/RenderingServer.java:277** [SECURITY] — REST API-key auth uses non-constant-time String.equals (timing attack), inconsistent with WS path
  - The /api/* authentication filter validates the bearer token with config.security().apiKey().equals(providedKey) (line 277). String.equals short-circuits on the first differing byte, leaking key-prefix correctness through response timing — a classic API-key timing oracle. The WebSocket auth path on the same server (line 339) was deliberately hardened with MessageDigest.isEqual constant-time comparison and per-host brute-force rate limiting (authLimiters), but the REST path got neither: no constant-time compare and no AuthAttemptRateLimiter. An attacker can brute-force the REST key byte-by-byte with no lockout. This is a real, exploitable inconsistency, not theoretical, given the WS path proves the team knows the correct pattern.
  - _Fix:_ Compare with MessageDigest.isEqual on UTF-8 bytes of ("Bearer "+apiKey) vs the raw header, exactly as the WS path does, and route the REST path through the same AuthAttemptRateLimiter keyed by ctx.ip() so failed REST auth attempts are also rate-limited.
- **/Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/viz/render/EntityStreamConsumer.java:179** [SECURITY] — Unbounded StringBuilder accumulation of upstream WebSocket frames — OOM / memory-exhaustion DoS
  - The upstream WebSocket Listener.onText appends every fragment into messageBuffer (StringBuilder) and only flushes when last==true (lines 178-185). There is no maximum-message-size guard. A malicious or buggy upstream (or a MITM, since the upstream URI may be plaintext ws://) can stream an unbounded sequence of non-final fragments and exhaust the rendering server's heap. Note the server's own client-facing inbound path (RegionStreamer.onMessageInternal, lines 185-191) DOES enforce config.maxMessageSizeBytes() and closes the session on overflow — so the protection pattern exists in this very module but was not applied to the upstream-consumer direction. This is silent scope reduction of the message-size defense to only one of two inbound paths.
  - _Fix:_ Track accumulated byte length in onText and abort/close the upstream WebSocket (and trigger reconnectWithBackoff) once it exceeds a configured cap (reuse PerformanceConfig / a new maxUpstreamMessageBytes), mirroring RegionStreamer's size check; reset messageBuffer on overflow.
- **simulation/src/main/java/com/hellblazer/luciferase/simulation/tumbler/BubbleMigrator.java:266-269, 220-225** [hollow-stub / silent-corruption] — getServerForBubble() always returns null -> source server metrics never decremented
  - getServerForBubble(Bubble) is a placeholder that unconditionally returns null ('In real implementation, look up from registry'). executeMigration step 6 calls tumbler.getServerMetrics(getServerForBubble(sourceBubble)) at line 220, so sourceMetrics is always null and removeBubble() is never invoked. After every migration the source server's bubble/entity counts keep growing forever, permanently skewing utilization. runMigrationCycle/findMigrationCandidates consume exactly these metrics, so the load balancer makes decisions on monotonically-corrupted data -- defeating the migrator's purpose while reporting MigrationResult success.
  - _Fix:_ Resolve the source server id from the real registry (or pass it into executeMigration). Until then the metrics-update step is wrong; decrement source metrics using a real lookup. Add a test asserting source utilization drops after a migration.
- **simulation/src/main/java/com/hellblazer/luciferase/simulation/tumbler/BubbleMigrator.java:199-217, 239-243** [concurrency / data-loss] — Non-atomic bubble migration: entities can exist in both bubbles on partial failure
  - executeMigration adds all entities to the target bubble (transferEntities, line 206) and only closes the source at step 5 (line 217). If broadcastMove(), the Thread.sleep, or any step between 206 and 217 throws, control falls to the catch at 239 which returns failure WITHOUT removing entities from the target or restoring the source. There is no two-phase commit / rollback, so a mid-flight failure duplicates every entity (present in both source and target) and the entity count metrics double-count. This is the classic migration split-brain.
  - _Fix:_ Make the transfer transactional: stage on target, only commit (and close source) after neighbor ACKs, and on any failure roll back the target staging. Mark source MIGRATING_OUT and target as authoritative atomically (the WAL ENTITY_DEPARTURE/MIGRATION_COMMIT pair exists for this -- use it).
- **portal/src/main/java/com/hellblazer/luciferase/portal/mesh/Face.java:262-265** [HOLLOW STUB / silent data loss] — setAllNormalIndices is a self-copy no-op — normal indices are never written
  - setAllNormalIndices(int... normalIndices) does System.arraycopy(normalIndices, 0, normalIndices, 0, normalIndices.length). The parameter name shadows the field this.normalIndices, so the source and destination are the SAME array — a no-op self-copy. The face's normalIndices field is never updated. Any caller relying on this to set per-vertex normal indices (e.g. divideIntoTriangles() copies normalIndices into triangles at line 150, and toOBJString() emits them at line 345) silently gets the default-zero / stale indices. The method also looks implemented (returns this, fluent) so the bug is invisible at the call site. Contrast with the correct setAllVertexIndices at line 272 which copies into the field vertexIndices.
  - _Fix:_ Copy the parameter into the field: System.arraycopy(normalIndices, 0, this.normalIndices, 0, normalIndices.length); (rename the parameter or qualify the destination with this.normalIndices). Add a test asserting getNormalIndices() reflects the argument and that it differs from the input array identity.
- **portal/src/main/java/com/hellblazer/luciferase/portal/web/SpatialInspectorServer.java:184-207, 633-648** [resource-leak] — Session deletion/server-stop leaks GPU native + spatial-index resources (SpatialSession.close() is a hollow stub)
  - deleteSession() and stop() only call session.close(), which is a no-op placeholder (SpatialSession.java:46-50 "Placeholder for resource cleanup"). The actual heavyweight resources are held in the *services*, keyed by sessionId: GpuService.sessions holds an ESVTOpenCLRenderer (native OpenCL/FFM buffers, disposed only via disableGpu()), SpatialIndexService holds the Octree/Tetree/Pyramid, RenderService holds ESVT/ESVO data. Neither deleteSession nor stop ever calls gpuService.disableGpu(id), spatialService.deleteIndex(id), or renderService.deleteRender(id). Result: every created-then-deleted session permanently leaks its GPU renderer (native memory) and spatial index. Over the server lifetime this is unbounded native-memory growth -> crash.
  - _Fix:_ Make SpatialSession actually own/cleanup, OR (simpler) in deleteSession() and stop() iterate and call gpuService.disableGpu(id) (guarded by isGpuEnabled), renderService.deleteRender(id) (guarded by hasRender), spatialService.deleteIndex(id) (guarded by hasIndex) for each removed session. Keep the no-op close() honest by removing the false 'resource cleanup' promise or wiring it to the services.
- **portal/src/main/java/com/hellblazer/luciferase/portal/web/SpatialInspectorServer.java:36, 150-159, 591-600** [security] — No session cap and no session reaping — unbounded sessions map is a memory-exhaustion DoS
  - createSession() unconditionally puts a new entry into the unbounded sessions ConcurrentHashMap with no cap on concurrent sessions. SESSION_TIMEOUT_MS (30 min) is defined but only ever used to compute an 'expired' boolean in the getSession response (line 180) — there is no background reaper and validateSession() does NOT reject expired sessions, it just touch()es them. An unauthenticated client can POST /api/session/create in a loop to exhaust heap; expired sessions are never evicted, and each can carry a leaked GPU renderer + spatial index (see Critical above). Combined with maxRequestSize=10MB and no per-session entity cap, amplification is severe.
  - _Fix:_ Add a max-session cap (reject 429 when exceeded) and a scheduled reaper (ScheduledExecutorService injected, Clock-driven) that evicts sessions where isExpired(SESSION_TIMEOUT_MS) and fully frees their service-side resources. Reject (not touch) expired sessions in validateSession().
- **portal/src/main/java/com/hellblazer/luciferase/portal/web/dto/GpuEnableRequest.java:13-21** [security] — Unbounded frameWidth/frameHeight -> int overflow / multi-GB native allocation (DoS)
  - GpuEnableRequest exposes frameWidth/frameHeight with no upper bound. GpuService.enableGpu() (GpuService.java:76-84) constructs new ESVTOpenCLRenderer(width,height), and render() (line 136) allocates new byte[width*height*4]. A request like frameWidth=100000,frameHeight=100000 makes width*height*4 overflow int to a negative value (NegativeArraySizeException / crash) or, at moderate-but-large sizes, allocates many GB of native+heap memory per session. No validation in DTO or service. benchmark() bounds iterations (1..100) but frame dimensions are never bounded.
  - _Fix:_ Clamp/validate frameWidth/frameHeight to a sane maximum (e.g. <= 4096 each) in getFrameWidthOrDefault/Height or in enableGpu(); reject with IllegalArgumentException (-> 400) otherwise. Compute the buffer size as long and check before allocating.

## Downgraded on verify (Medium/Low)

- geometry/src/main/java/com/hellblazer/luciferase/geometry/rd/RDGCoordinates.java:149-174 [Low] faceConnectedNeighbors javadoc mislabels the 12 offsets as '6 face + 6 second-shell' — all 12 are first-shell (distance 1)
- lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/balancing/grpc/BalanceCoordinatorClient.java:364 [Medium] Streaming connection id derived from wall-clock with no counter -> collision drops/leaks streams
- lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/GhostCoordinator.java:231-292 [Medium] findEntitiesIncludingGhosts / findNeighborsIncludingGhosts silently return zero ghosts
- lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/ContentSerializer.java:104-116 [Low] STRING_SERIALIZER cannot round-trip an empty string — "" deserializes to null
- lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/GhostBoundaryDetector.java:456-471 [Medium] removeGhostsForElement is empty — updateElementGhosts cannot remove stale ghosts
- lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/fault/SimpleFaultHandler.java:264-302 [Medium] TOCTOU race in fault status escalation (report* methods)
- common/src/main/java/com/hellblazer/luciferase/common/OpenAddressingSet.java:50, 84-96, 212-224 [Low] Rehash trigger ignores tombstones — table can saturate with DELETED, degrading probe to O(n) and breaking the `index != hash` termination invariant
- common/src/main/java/com/hellblazer/luciferase/common/FloatArrayList.java:181-188 [Medium] removeRange() does not validate fromIndex/toIndex bounds — silent corruption or ArrayIndexOutOfBounds
- /Users/hal.hildebrand/git/Luciferase/dyada-java/src/main/java/com/dyada/performance/DyAdaCache.java:73-84, 112-130 [Medium] Cache size bound and LRU eviction are racy and non-atomic across two ConcurrentHashMaps
- /Users/hal.hildebrand/git/Luciferase/dyada-java/src/main/java/com/dyada/transformations/LinearTransformation.java:383-406 [Medium] Inverse transformation sets final fields via reflection, defeating final semantics and JPMS encapsulation
- /Users/hal.hildebrand/git/Luciferase/lucien/src/main/java/com/hellblazer/luciferase/lucien/ParallelBulkOperations.java:52-53, 70-74, 229-237 [Medium] Owns ForkJoinPool + non-daemon fixedThreadPool but is not AutoCloseable; owner AbstractSpatialIndex also not AutoCloseable
- /Users/hal.hildebrand/git/Luciferase/lucien/src/main/java/com/hellblazer/luciferase/lucien/ParallelBulkOperations.java:239-247 [Medium] updateBatchParallel silently reassigns NEW entity IDs on 'update', invalidating caller-held IDs
- lucien/src/main/java/com/hellblazer/luciferase/lucien/tetree/TetreeRegionCache.java:52-57 [Low] packCacheKey truncates world coordinates to 12 bits — silent wrong cached key for coords > 4095
- lucien/src/main/java/com/hellblazer/luciferase/lucien/octree/OctreeBalancer.java:184, 222, 234 [Medium] rebalanceTree() calls System.nanoTime() directly in production code
- lucien/src/main/java/com/hellblazer/luciferase/lucien/sfc/LitmaxBigmin.java:184-201 [Medium] findIntervalEnd() linearly scans the entire Morton range, defeating the BIGMIN optimization and risking O(range) blowups
- lucien/src/main/java/com/hellblazer/luciferase/lucien/occlusion/HierarchicalZBuffer.java:181-212 [Medium] public updateHierarchy() mutates zBuffers with no lock
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/io/ESVODeserializer.java:131-133 [Medium] Deserializer allocates ByteBuffer.allocate(nodeCount*12) from unvalidated header field; integer overflow / OOM
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/dag/cache/DAGCache.java:87-96, 91 [Low] removeEldestEntry mutates evictionCount outside the write lock guarantee and double-counts vs evict()
- render/src/main/java/com/hellblazer/luciferase/esvt/gpu/ESVTGPUMemory.java:241-271 [Medium] ESVTGPUMemory is not AutoCloseable and relies on deprecated finalize() to free native memory
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/von/transport/SocketServer.java:82-87 [Medium] Unbounded cached thread pool + no accepted-connection/session cap -> thread & FD exhaustion DoS
- simulation/src/main/java/com/hellblazer/luciferase/simulation/topology/BubbleSplitter.java:256-294 [Medium] Split conservation check reads accountant counts non-atomically (TOCTOU) under partial-move failure
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/bubble/BucketSynchronizedController.java:139, 151 [Low] synchronizeAtBucket/advanceBucket unconditional set() defeats the CAS guard the tick loop relies on
- simulation/src/main/java/com/hellblazer/luciferase/simulation/behavior/PackHuntingBehavior.java:187-188, 158 [Medium] Pursuit speed cap applied whenever prey exists, even when entity is in patrol/wander (prey beyond chaseRange)
- simulation/src/main/java/com/hellblazer/luciferase/simulation/consensus/committee/CommitteeServiceImpl.java:74-82, 123-143 [Medium] getQuorumResult cannot distinguish failed/timed-out proposals from still-pending ones
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/distributed/migration/CrossProcessMigration.java:152-184 [Medium] CrossProcessMigration owns a started RealTimeController + ScheduledExecutorService but is not AutoCloseable; leaks if stop() is not called
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/distributed/network/GrpcBubbleNetworkChannel.java:127-135 [Medium] A new JVM shutdown hook is registered on every initialize() call and never removed
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/ghost/GhostLifecycleStateMachine.java:170, 291-305, 319-333, 412-414 [Medium] Injected Clock is dead code — staleness/expiry use caller-supplied time, so setClock() does nothing
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/viz/render/RenderingServer.java:152 [Medium] RegionCache created in start() never receives setClock(clock) — clock injection silently incomplete
- portal/src/main/java/com/hellblazer/luciferase/portal/mesh/polyhedra/Polyhedron.java:42-53 [Medium] Copy constructor reads its own (empty) fields instead of the source polyhedron
- portal/src/main/java/com/hellblazer/luciferase/portal/web/service/SpatialIndexService.java:85, 115, 163, 274, 304 [Low] No coordinate validation (NaN/Infinity/negative) before inserting into SFC-indexed structures

## Unverified Medium/Low

- geometry/src/test/java/com/hellblazer/luciferase/geometry/rd/RDGCoordinatesTest.java:128-133 [Medium/test-gap] faceConnectedNeighbors test only checks count+distinctness, never the geometric invariant (distances)
- geometry/src/test/java/com/hellblazer/luciferase/geometry/rd/RDGCoordinatesTest.java:88-89 [Medium/test-gap] euclideanNorm test is vacuous — asserts isGreaterThan(0f) where an exact value is the real invariant
- geometry/src/main/java/com/hellblazer/luciferase/geometry/rd/RDGCoordinates.java:143-147 [Low/geometry] toRDG round-trip identity is only validated/guaranteed for |coord|<=3; float precision of Math.round inversion is undefended at larger magnitudes
- lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/balancing/grpc/BalanceCoordinatorServer.java:110, 202 [Medium/clock-injection] BalanceCoordinatorServer uses System.currentTimeMillis() directly (no injected Clock)
- lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/balancing/grpc/BalanceCoordinatorClient.java:167, 264 [Medium/clock-injection] BalanceCoordinatorClient stamps request timestamps with System.currentTimeMillis()
- lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/grpc/ProtobufConverters.java:204-207, 259-262 [Medium/clock-injection] ProtobufConverters stamps ghost timestamps with System.currentTimeMillis() and reads it twice non-atomically
- lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/grpc/GhostExchangeServiceImpl.java:166, 228-229, 281-282 [Medium/clock-injection] GhostExchangeServiceImpl uses System.currentTimeMillis() for session ids and sync/batch timestamps
- lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/balancing/grpc/BalanceCoordinatorClient.java:594-639 [Medium/resource-leak] BatchQueue creates a per-rank ScheduledExecutorService that defeats the documented batching and leaks the wall-clock-scheduled task
- lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/grpc/TetreeKeySerde.java:77-83 [Medium/serialization] TetreeKey deserialize silently drops high bits for level<=10 (asymmetric round-trip)
- lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/balancing/grpc/GrpcBalanceExchange.java:175-190 [Low/serialization] toDomainViolation casts deserialized keys to Key with unchecked cast and no type-id guard
- lucien-distributed/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/grpc/GhostServiceClient.java:206-234 [Low/design] requestGhostElements returns null vs empty-list with different meanings, undocumented at the GhostExchange interface boundary
- lucien/src/main/java/com/hellblazer/luciferase/lucien/collision/CollisionSystem.java:137, 187 [Medium/clock-injection] CollisionSystem.processAllCollisions uses System.nanoTime() directly (clock-injection mandate violation)
- lucien/src/main/java/com/hellblazer/luciferase/lucien/collision/physics/constraints/DistanceConstraint.java:112, 141-145 [Medium/bug] DistanceConstraint warm-start uses lambda*deltaTime but solver accumulates raw impulse (unit mismatch)
- lucien/src/main/java/com/hellblazer/luciferase/lucien/collision/CollisionSystem.java:419-429 [Medium/bug] CCD broad-phase seeds kNN only from the start position, missing collisions along long sweeps
- lucien/src/main/java/com/hellblazer/luciferase/lucien/collision/ccd/ContinuousCollisionDetector.java:36, 186-194 [Medium/bug] conservativeCCD fixed 32-step scan can tunnel collision windows narrower than motion/32
- lucien/src/main/java/com/hellblazer/luciferase/lucien/collision/ccd/ContinuousCollisionDetector.java:131-134 [Low/geometry] sphereVsSphereCCD contact point offset uses only sphere1 radius, biasing the contact off the true touch point
- lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/DistributedGhostManager.java:271, 460 [Medium/clock-injection] DistributedGhostManager uses System.currentTimeMillis() for sync scheduling
- lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/ForestLoadBalancer.java:46, 70 [Medium/clock-injection] TreeLoadMetrics captures system clock in a field initializer and divides by wall-clock age
- lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/AdaptiveForest.java:181 [Medium/clock-injection] AdaptiveForest tree-state lastUpdateTime field-initializer captures system clock
- lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/Forest.java:122-123, 152-154 [Medium/concurrency] addTree/removeTree mutate trees (CoWArrayList) and treeMap (CHM) non-atomically
- lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/Forest.java:527, 529 [Low/clock-injection] generateTreeId uses System.nanoTime() for ID entropy
- lucien/src/main/java/com/hellblazer/luciferase/lucien/forest/ghost/DistributedGhostManager.java:60-61 [Low/maintainability] spatialIndex field retained but unused — speculative API surface
- lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/fault/CascadingRecoveryImpl.java:487-491 [Medium/resource-leak] CascadingRecoveryImpl owns an ExecutorService but is not AutoCloseable
- lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/fault/CascadingRecoveryImpl.java:520 [Medium/clock-injection] notifyEvent uses RecoveryEvent.now() (System.currentTimeMillis) instead of the injected clock
- lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/fault/SimpleFaultHandler.java:63,72,238,250,344,382,433,450 [Medium/clock-injection] SimpleFaultHandler has no Clock injection — all timestamps use System.currentTimeMillis() directly
- lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/CrossPartitionBalancePhase.java:159,205,235,242,260,307,315,394,403,445,478 [Medium/clock-injection] CrossPartitionBalancePhase uses System.nanoTime()/currentTimeMillis() with no Clock field
- lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/RefinementRequestManager.java:83,134,136 [Medium/clock-injection] RefinementRequestManager computes round-trip time via System.currentTimeMillis() with no Clock field
- lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/DefaultBalancingStrategy.java:127,172-176 [Medium/bug] shouldMerge uses a hardcoded parent capacity (always 100), ignoring actual maxEntitiesPerNode
- lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/DefaultBalancingStrategy.java:74-88 [Low/design] distributeEntities is round-robin, ignoring spatial locality despite being the production strategy
- lucien/src/main/java/com/hellblazer/luciferase/lucien/balancing/fault/DefaultFailureDetector.java:41,53 [Low/resource-leak] DefaultFailureDetector owns a ScheduledExecutorService but is not AutoCloseable
- common/src/main/java/com/hellblazer/luciferase/common/OpenAddressingSet.java:26, 31-33, 44, 105-144 [Medium/Concurrency / API contract] OpenAddressingSet is non-thread-safe with mutable iterator capture of `table` field — undocumented and footgun-prone in a concurrent codebase
- common/src/main/java/com/hellblazer/luciferase/common/FloatArrayList.java:88-95 [Medium/Performance / latent] addAll(FloatArrayList) grows backing array to exact newSize — O(n^2) on repeated appends
- common/src/main/java/com/hellblazer/luciferase/common/OpenAddressingSet.java:57-60 [Low/Correctness / consistency] clear() nulls the table but leaves `load` unchanged, then add() re-inits to capacity 1
- /Users/hal.hildebrand/git/Luciferase/dyada-java/src/main/java/com/dyada/transformations/LinearTransformation.java:416 [Medium/CORRECTNESS] composeWithLinear allocates result matrix with wrong column count for non-square composition
- /Users/hal.hildebrand/git/Luciferase/dyada-java/src/main/java/com/dyada/refinement/AdaptiveMesh.java:235, 277 [Medium/CLOCK-INJECTION] refineAdaptively measures executionTime with System.currentTimeMillis() instead of injected Clock
- /Users/hal.hildebrand/git/Luciferase/dyada-java/src/main/java/com/dyada/refinement/GradientBasedRefinement.java:164-187 [Medium/CORRECTNESS / silent-scope-reduction] validateRefinementDecisions only inspects dimension 0 of each LevelIndex despite a multi-dimensional level model
- /Users/hal.hildebrand/git/Luciferase/dyada-java/src/main/java/com/dyada/performance/MortonOptimizer.java:196-198 [Medium/CORRECTNESS] mortonDistance2D returns 0 for identical codes and is a quadtree-LCA depth, not a distance
- /Users/hal.hildebrand/git/Luciferase/dyada-java/src/main/java/com/dyada/performance/DyAdaCache.java:41-57 [Low/CONCURRENCY / TTL] get(key, loader) computes `now` but loader runs without dedup, allowing redundant concurrent loads
- /Users/hal.hildebrand/git/Luciferase/dyada-java/src/main/java/com/dyada/transformations/AffineTransformation.java:154-174 [Low/DESIGN / correctness-clarity] compose() composition order is undocumented and inconsistent with mathematical convention risk
- sentry/src/main/java/com/hellblazer/sentry/Tetrahedron.java:circumsphereRadius() — Geometry.centerSphereFast [Medium/GEOMETRY/numerical-robustness] circumsphereRadius uses centerSphereFast (float, non-adaptive) for Delaunay validation
- sentry/src/main/java/com/hellblazer/sentry/MutableGrid.java:229-281 (rebuildOptimized) and 308-320 (track with near) [Medium/DESIGN/silent-data-loss] rebuild and track silently drop points whose locate returns null
- sentry/src/main/java/com/hellblazer/sentry/MutableGrid.java:231,103-108,95 (System.getProperty allocation/rebuild strategy) [Low/DESIGN] Allocation/rebuild strategy read from global System.getProperty at runtime in hot path
- sentry/src/main/java/com/hellblazer/sentry/LandmarkIndex.java:50-53,597 (MutableGrid.initialize) — new Random() seed [Low/DESIGN/determinism] LandmarkIndex constructed with unseeded new Random() — non-deterministic landmark selection
- /Users/hal.hildebrand/git/Luciferase/lucien/src/main/java/com/hellblazer/luciferase/lucien/SpatialIndexSet.java:297-316, 180-195, 34, 43 [Medium/concurrency/dead-code] NavigableSet impl returns stale lazily-cached sortedView; modificationCount/SORT_THRESHOLD dead; class effectively unused
- /Users/hal.hildebrand/git/Luciferase/lucien/src/main/java/com/hellblazer/luciferase/lucien/SpatialNodePool.java:185-204, 86-109 [Medium/concurrency] release()/acquire() size accounting is TOCTOU: pool can exceed maxSize and currentSize can drift
- /Users/hal.hildebrand/git/Luciferase/lucien/src/main/java/com/hellblazer/luciferase/lucien/AbstractSpatialIndex.java:170, 1447-1462, 1786-1787 [Medium/concurrency] lastBalancingTime is a plain non-volatile long read/written outside any lock in checkAutoBalance
- /Users/hal.hildebrand/git/Luciferase/lucien/src/main/java/com/hellblazer/luciferase/lucien/SpatialNodePool.java:230-240 [Low/concurrency] shrink() polls and decrements non-atomically; can under/over-count currentSize
- lucien/src/main/java/com/hellblazer/luciferase/lucien/internal/EntityCache.java:82-95 [Medium/concurrency-race] EntityCache put eviction TOCTOU and approximateSize drift under concurrent access
- lucien/src/main/java/com/hellblazer/luciferase/lucien/migration/SpatialIndexConverter.java:53,76,98,121 [Medium/clock-injection] SpatialIndexConverter uses System.currentTimeMillis() for duration in production code
- lucien/src/main/java/com/hellblazer/luciferase/lucien/internal/ObjectPools.java:45-49 [Medium/resource-correctness] borrowArrayList(initialCapacity) may hand back a list already containing elements; ensureCapacity is misleading
- lucien/src/main/java/com/hellblazer/luciferase/lucien/migration/SpatialIndexConverter.java:245-249 [Medium/vacuous-error-path] Conversion swallows all per-entity exceptions into a warn log; caller cannot distinguish lossy from clean conversion
- lucien/src/main/java/com/hellblazer/luciferase/lucien/internal/ObjectPools.java:36-39,65-67 [Low/design-unchecked-cast] Unchecked generic casts in object pool can hand back a list/set of the wrong element type
- lucien/src/main/java/com/hellblazer/luciferase/lucien/tetree/TetreeLevelCache.java:245-261, 364-375, 320-331 [Medium/GEOMETRY] generateCacheKey hash path is lossy — full-key collision returns a wrong cached key for large coordinates
- lucien/src/main/java/com/hellblazer/luciferase/lucien/tetree/TetreeLevelCache.java:81-86, 269-270, 369, 373 [Low/CONCURRENCY] Non-atomic statistics counters on a concurrently-accessed static cache
- lucien/src/main/java/com/hellblazer/luciferase/lucien/tetree/SpatialLocalityCache.java:59, 79 [Low/DESIGN] preCacheNeighborhood computes cacheCount but never uses it; SpatialLocalityCache has no own storage
- lucien/src/main/java/com/hellblazer/luciferase/lucien/octree/OctreeBalancer.java:177-180 [Medium/HOLLOW STUB] rebalanceSubtree() is a hollow stub that always returns 0
- lucien/src/main/java/com/hellblazer/luciferase/lucien/sfc/SFCArrayIndex.java:143-167 [Medium/correctness] cellsQ() floors maxX/Y/Z but does not clamp to MAX_COORD grid bound, can encode out-of-range cells
- lucien/src/main/java/com/hellblazer/luciferase/lucien/octree/MortonKey.java:561-575 [Medium/correctness] estimateSFCRange builds a Morton range from raw AABB corners — interior in-sphere cells can fall outside [min,max]
- lucien/src/main/java/com/hellblazer/luciferase/lucien/prism/PrismKey.java:146-174 [Medium/concurrency] consecutiveIndex() cache: volatile long avoids torn read but benign recompute relies on undocumented determinism of two delegate caches
- lucien/src/main/java/com/hellblazer/luciferase/lucien/prism/Triangle.java:406-421 [Medium/GEOMETRY] isPointInTriangle uses float barycentric test with no shared-edge tie-break — diagonal points classified inconsistently with fromWorldCoordinates
- lucien/src/main/java/com/hellblazer/luciferase/lucien/prism/Triangle.java:435-498 [Low/DESIGN] Deprecated neighbors()/neighbor() retain S0-bounded approximation that silently returns null at the S0/S1 diagonal
- lucien/src/main/java/com/hellblazer/luciferase/lucien/sfc/LitmaxBigmin.java:263-265 [Low/correctness] bigmin() 'curBit==1 && minBit==0 && maxBit==0' branch breaks without ever advancing past current in some prefixes
- lucien/src/main/java/com/hellblazer/luciferase/lucien/octree/OctreeBalancer.java:182-237 [Low/DESIGN] rebalanceTree() swallows all exceptions and reports failure as a success-shaped RebalancingResult
- lucien/src/main/java/com/hellblazer/luciferase/lucien/occlusion/HierarchicalZBuffer.java:419-436 [Medium/clock-injection/concurrency] adaptResolution uses System.currentTimeMillis() and mutates lastAdaptationTime outside any lock
- lucien/src/main/java/com/hellblazer/luciferase/lucien/occlusion/HierarchicalOcclusionCuller.java:330-336 [Medium/clock-injection/concurrency] Frame timing uses System.nanoTime() and races on plain frameStartTime field
- lucien/src/main/java/com/hellblazer/luciferase/lucien/neighbor/MortonNeighborDetector.java:126-137 [Medium/hollow-stub] findNeighborsWithOwners hardcodes local ownership for every neighbor
- lucien/src/main/java/com/hellblazer/luciferase/lucien/neighbor/MortonNeighborDetector.java:150-152 [Medium/correctness/edge-case] Neighbor bounds check excludes the maximum-coordinate cell
- lucien/src/main/java/com/hellblazer/luciferase/lucien/neighbor/MortonNeighborDetector.java:75-160 [Medium/design/correctness] Neighbors returned without checking existence in the octree
- lucien/src/main/java/com/hellblazer/luciferase/lucien/occlusion/HierarchicalZBuffer.java:337-359 [Medium/correctness/geometry] testOcclusionAtLevel index mismatch when buffer width is not a power of two
- lucien/src/main/java/com/hellblazer/luciferase/lucien/cache/KNNCache.java:224-229 [Low/concurrency] getHitRate reads atomics outside the cache lock (documented but inconsistent with getStats)
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/dag/DAGBuilder.java:385-416 (buildMetadata), 392-397, 399-400 [Medium/HOLLOW_STUB] DAGMetadata sharing-by-depth and sourceHash are placeholders presented as real metadata
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/dag/DAGBuilder.java:424-471 (estimateMaxDepth), 456-457 [Medium/GEOMETRY / correctness] estimateMaxDepth assumes node.getChildOffset(octant) matches the compacted childPointers layout it does not control
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/core/ESVONode.java:187-201 (getChildNodeIndexWithOctant) [Medium/GEOMETRY / correctness] getChildNodeIndexWithOctant existence test uses octant-shifted descriptor but sparse index uses unshifted mask -- inconsistent octant mirroring
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/dag/cache/LazyDAGCompressor.java:189, 193 [Medium/CLOCK_INJECTION] LazyDAGCompressor computes timeout deadline via System.currentTimeMillis() (direct clock call in prod)
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/dag/metrics/CompressionMetrics.java:92 [Medium/CLOCK_INJECTION] CompressionMetrics timestamp via direct System.currentTimeMillis()
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/optimization/ESVOOptimizationProfiler.java:24, 38, 57, 77 [Low/CLOCK_INJECTION] ESVOOptimizationProfiler uses System.nanoTime() directly (multiple sites)
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/io/ESVOSerializer.java:79-126 (serializeWithMetadata) [Low/DESIGN / edge-case] serializeWithMetadata header rewrite assumes fixed header layout and reuses a flipped buffer fragilely
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/profiler/GPUPerformanceProfiler.java:149, 175, 215, 269, 324, 388, 238, 291, 343 [Medium/clock-injection] GPUPerformanceProfiler calls System.currentTimeMillis()/nanoTime() directly (mandate violation)
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/GPUBufferPool.java:57, 144 [Medium/clock-injection] GPUBufferPool uses System.nanoTime() directly for allocation/LRU timestamps
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/profiler/GPUPerformanceProfiler.java:258, 313, 377 [Medium/logging-correctness] SLF4J log statements use Python-style {:.2f}/{:.1f}/µs format specifiers that SLF4J cannot interpret
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/ESVOOpenCLRenderer.java:42, 98-114, 120-142, 144-158 [Medium/silent-scope-reduction / far-pointer protocol] ESVOOpenCLRenderer advertises 'Far pointer support for large octrees' but uploads no far-pointer buffer and never wires one to the kernel
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/DAGOpenCLRenderer.java:546-580 [Medium/concurrency / shared-mutable-state] executeKernel temporarily mutates the shared 'kernel' field to swap in batchKernel (not thread-safe)
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/OctreeGPUMemory.java:243-250, 29, 218 [Medium/resource-leak / lifecycle] OctreeGPUMemory is not AutoCloseable and relies on deprecated finalize() as its leak backstop
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/DAGOpenCLRenderer.java:769-830 [Medium/hollow-stub] optimizeForDevice()/detectGPUCapabilities() hard-code a fake NVIDIA 'Generic GPU' instead of querying the device
- /Users/hal.hildebrand/git/Luciferase/render/src/main/java/com/hellblazer/luciferase/esvo/gpu/GPUBufferPool.java:220-225 [Low/concurrency] touch() mutates activeBuffers (ConcurrentHashMap) outside the pool's write lock used by allocate/release
- render/src/main/java/com/hellblazer/luciferase/esvt/optimization/ESVTTraversalOptimizer.java:376-398 [Medium/HOLLOW STUB] estimateIntersectionStats returns fabricated numbers dressed as measured intersection statistics
- render/src/main/java/com/hellblazer/luciferase/esvt/optimization/ESVTTraversalOptimizer.java:458-476 [Medium/GEOMETRY / logic bug] classifyDominantTetFace never returns a Z-axis face and aliases +Z onto face 0
- render/src/main/java/com/hellblazer/luciferase/esvt/builder/ESVTBuilder.java:624-627 [Medium/DESIGN / silent override] propagateTypesTopDown hardcodes root type 0, silently overriding the key-derived root type used elsewhere
- render/src/main/java/com/hellblazer/luciferase/esvt/builder/ESVTBuilder.java:569-588 [Medium/HOLLOW STUB / scope reduction] computeMortonChildIndex throws on pyramid-rooted tets — builder cannot handle the production-live pyramid boundary path
- render/src/main/java/com/hellblazer/luciferase/esvt/io/ESVTDeserializer.java:148-208 [Medium/CORRECTNESS / partial read] FileChannel.read() return value ignored — short reads silently produce truncated/zero-filled node and table data
- render/src/main/java/com/hellblazer/luciferase/esvt/core/ESVTData.java:177-183 [Medium/CORRECTNESS / silent fallback] resolveChildPtr silently returns the raw far-index when far pointers are missing or index out of range
- render/src/main/java/com/hellblazer/luciferase/esvt/traversal/ESVTChildOrder.java:169-194 [Low/GEOMETRY / maintainability] CHILD_CENTROIDS octahedral children 4-7 use a hand-written tetra decomposition not cross-checked against TetreeConnectivity Bey tables
- render/src/main/java/com/hellblazer/luciferase/render/benchmark/Phase5a5BenchmarkRunner.java:103-113 [Medium/fabricated-metric / benchmark-validity] CameraMovement benchmark persists hardcoded coherence (0.6) and zeroed node counts as a real result
- render/src/main/java/com/hellblazer/luciferase/render/tile/AdaptiveScheduler.java:149-197 [Medium/concurrency / unguarded shared mutable state] AdaptiveScheduler.decide() does read-modify-write on unsynchronized instance fields
- render/src/main/java/com/hellblazer/luciferase/render/mesh/MeshLoader.java:38 [Low/clock-injection / logging] MeshLoader logs via System.out.println(System.currentTimeMillis()) instead of SLF4J/injected Clock
- render/src/main/java/com/hellblazer/luciferase/render/tile/HybridTileDispatcher.java:193-208 [Low/design / misleading abstraction] 'Hybrid' CPU/GPU dispatcher executes GPU then CPU strictly sequentially
- render/src/main/java/com/hellblazer/luciferase/sparse/gpu/AbstractOpenCLRenderer.java:438-461 [Low/resource-leak / API contract] createRawBuffer/createEmptyRawBuffer hand out untracked cl_mem with no base-class release tracking
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/von/transport/SocketTransport.java:137 [Medium/clock-injection] SocketTransport hardwires MessageFactory.system() -> ACK timestamps use wall clock, non-injectable for deterministic tests
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/von/transport/SocketClient.java:99-106 [Medium/resource-leak] SocketClient.connect() uses no connect timeout; close() leaves outStream unclosed
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/von/MessageConverter.java:481-492 [Medium/serialization] EntitySnapshot.content collapsed to String on migration round-trip -> silent data loss for non-String content
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/von/MessageConverter.java:294-303 [Low/design] Ack to/from-transport swaps sourceBubbleId/targetBubbleId semantics in a non-obvious way (latent field-mapping fragility)
- simulation/src/main/java/com/hellblazer/luciferase/simulation/topology/TopologyConsensusCoordinator.java:342-372, 394-408 [Medium/concurrency] Cooldown reservation ABA token is the clock millis 'now'; same-tick concurrent proposals can collide on restore
- simulation/src/main/java/com/hellblazer/luciferase/simulation/topology/TopologyConsensusCoordinator.java:255-263, 244-250, 426-434, 276-306 [Medium/hollow-stub] toMigrationProposal maps bubble UUIDs to non-member digests (rejected by real consensus); dead cooldown methods
- simulation/src/main/java/com/hellblazer/luciferase/simulation/topology/BubbleMerger.java:102-238 [Medium/concurrency] Merge has no rollback of moved entities and reads counts non-atomically; same partial-move and TOCTOU class as splitter
- simulation/src/main/java/com/hellblazer/luciferase/simulation/topology/SplitCooldownTracker.java:120-136, 190-195 [Low/concurrency] isOnCooldown / getActiveCooldownCount use non-atomic get-then-remove and bulk removeIf with separate 'now' reads
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/bubble/TetrahedralMigration.java:164-182 [Medium/HOLLOW STUB] Documented hysteresis (HYSTERESIS_DIST=2.0f) is never enforced — migrationCandidate always returns true
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/bubble/BubbleGhostCoordinator.java:53-62 [Medium/HOLLOW STUB] Ghost dead-reckoning fed hardcoded zero velocity — reckoning cannot move ghosts
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/bubble/TetrahedralMigration.java:232-269 [Medium/CONCURRENCY] Two-phase migration has no cross-bubble lock — read-add-remove can interleave with concurrent updates/migrations
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/bubble/EnhancedBubble.java:371-378 [Medium/HOLLOW STUB] EnhancedBubble.tick(long bucket) is a no-op placeholder despite per-tick contract in javadoc
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/bubble/BubbleEntityStore.java:80, 150 [Low/CONCURRENCY] Caller-owned Point3f stored by reference into BubbleEntityData (aliasing / mutation hazard)
- simulation/src/main/java/com/hellblazer/luciferase/simulation/animation/VolumeAnimator.java:170-183 [Medium/concurrency] AnimationFrame.track() unconditionally self-recurses with no termination or stop condition
- simulation/src/main/java/com/hellblazer/luciferase/simulation/animation/EnhancedVolumeAnimator.java:71, 144, 162 [Medium/concurrency] frameCount is non-volatile and incremented non-atomically despite documented thread-safe concurrent access
- simulation/src/main/java/com/hellblazer/luciferase/simulation/behavior/PackHuntingBehavior.java:100-103 [Medium/design] Deprecated no-arg constructor seeds an unseeded Random, defeating deterministic simulation
- simulation/src/main/java/com/hellblazer/luciferase/simulation/animation/EnhancedVolumeAnimator.java:113-118 [Low/concurrency] TOCTOU between getActiveGhosts() snapshot and per-ghost getGhostPosition()
- simulation/src/main/java/com/hellblazer/luciferase/simulation/consensus/committee/CommitteeServiceImpl.java:53, 74-82, 135-139 [Medium/resource-leak] proposalResults grows unbounded for any proposal whose result is never queried
- simulation/src/main/java/com/hellblazer/luciferase/simulation/consensus/committee/CommitteeBallotBox.java:173-213 [Medium/correctness/determinism] Even committee size produces a nondeterministic consensus result on a split tally
- simulation/src/main/java/com/hellblazer/luciferase/simulation/consensus/committee/CommitteeVotingProtocol.java:239 [Medium/concurrency] rollbackOnViewChange uses getResult (resurrecting) instead of getResultIfPresent, inconsistent with the zombie-future guard
- simulation/src/main/java/com/hellblazer/luciferase/simulation/causality/EventReprocessor.java:processReady catch block (processor.process failure path) [Medium/silent-data-loss] Event whose processor throws is removed from queue and tracker but counted as neither reprocessed nor dropped
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/distributed/migration/MigrationLogPersistence.java:248-250 [Medium/serialization-silent-data-loss] WAL recovery silently skips malformed entries, dropping in-flight transactions on crash recovery
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/distributed/network/GrpcBubbleNetworkChannel.java:404-420 [Medium/security] Client gRPC channel has no maxInboundMessageSize cap (DoS surface on responses)
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/distributed/network/GrpcBubbleNetworkChannel.java:417 [Medium/security] gRPC transport uses usePlaintext() with no TLS / peer authentication in a distributed migration path
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/distributed/network/GrpcBubbleNetworkChannel.java:195-199 [Low/correctness] sendWithRetry onNext swallows handler exceptions with empty catch (Exception ignored)
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/ghost/GhostLifecycleStateMachine.java:92-93, 33-47, 97-106, 234-251 [Medium/silent-scope-reduction / state-machine-doesnt-machine] STALE state is never reached: default staleness threshold == TTL (both 500ms) and stored state never transitions to STALE
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/ghost/DelosSocketTransport.java:193-208, 210-213, 260-264 [Medium/serialization-silent-data-loss (RDR-004 class)] sendBatch swallows per-ghost serialization failures and ACKs success; isConnected always true
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/ghost/GhostStateManager.java:192, 295-297, 410-412, 524-526 [Medium/concurrency] metrics field is non-volatile but read on hot paths from arbitrary threads
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/ghost/InMemoryGhostChannel.java:135-141, 188-195 [Low/resource-leak / silent-drop on close] close() shutdownNow() drops in-flight latency-delayed ghost batches without report
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/viz/render/RenderingServer.java:103 [Medium/CLOCK INJECTION] EntityStreamConsumer constructed with hardcoded Clock.system() in RenderingServer constructor
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/viz/render/RenderingServer.java:360 [Medium/CONCURRENCY] app.start() opens the listener and consumers before all WS-handler state is guaranteed published
- /Users/hal.hildebrand/git/Luciferase/simulation/src/main/java/com/hellblazer/luciferase/simulation/viz/render/protocol/BinaryFrameCodec.java:171 [Low/SERIALIZATION SILENT DATA LOSS] decodeHeader trusts dataSize field with no bound check vs buffer.remaining()
- simulation/src/main/java/com/hellblazer/luciferase/simulation/persistence/EventRecovery.java:220 [Medium/vacuous-test / dead-check] validateRecoveryIntegrity reads 'sequenceNumber' but WAL events carry 'sequence' -> monotonicity gate is dead
- simulation/src/main/java/com/hellblazer/luciferase/simulation/tumbler/BubbleMigrator.java:214 [Medium/hollow-stub / clock-injection] Migration 'wait for neighbor ACKs' is a hardcoded Thread.sleep(50)
- simulation/src/main/java/com/hellblazer/luciferase/simulation/persistence/WriteAheadLog.java:415-425 [Medium/serialization / data-loss] readLogFile silently skips every malformed line, not just a torn tail
- simulation/src/main/java/com/hellblazer/luciferase/simulation/lifecycle/ViewStabilityGate.java:93-144 [Low/resource-leak] awaitStability creates a new ScheduledExecutorService per call
- portal/src/main/java/com/hellblazer/luciferase/portal/mesh/util/Canonicalize.java:116-128 [Medium/correctness / dead safety guard] planarize(int) computes maxChange from diff(newPos,newPos) — always 0, divergence guard dead
- portal/src/main/java/com/hellblazer/luciferase/portal/mesh/Edge.java:52-57 [Medium/correctness / crash] Edge.equals lacks null and type guards — ClassCastException / NPE in hash collections
- portal/src/main/java/com/hellblazer/luciferase/portal/mesh/Edge.java:136-139 [Low/performance / hash distribution] Edge.hashCode multiplicative with vertex index 0 collapses to 0 and collides heavily
- portal/src/main/java/com/hellblazer/luciferase/portal/mesh/Mesh.java:237-254 [Low/resource / error handling] toObjFile leaks FileWriter on exception and swallows IOException via printStackTrace
- portal/src/main/java/com/hellblazer/luciferase/portal/mesh/spatial/TetreeView.java:118-212 [Low/logging convention] Production view code uses System.out.println for diagnostics instead of SLF4J
- portal/src/main/java/com/hellblazer/luciferase/portal/web/SpatialInspectorServer.java:269-280 [Medium/security] bulk-insert has no element-count cap (10MB body -> unbounded per-session entity growth)
- portal/src/main/java/com/hellblazer/luciferase/portal/esvt/ESVTInspectorApp.java:811-813 [Medium/design] Direct main() calls Application.launch(args) on the Application subclass — violates mandated Launcher-inner-class pattern
- portal/src/main/java/com/hellblazer/luciferase/portal/esvo/bridge/ESVOBridge.java:227-231 [Medium/serialization] buildFromVoxels returns a non-null BuildResult wrapping a null structure on failure — looks successful to unchecked callers
- portal/src/main/java/com/hellblazer/luciferase/portal/web/service/GpuService.java:125-127, 186-188 [Low/clock-injection] Direct System.nanoTime() in production web-service render/benchmark path