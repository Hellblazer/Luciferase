package com.hellblazer.luciferase.simulation.span;

import com.hellblazer.luciferase.lucien.entity.EntityID;

/**
 * Manages boundary zones between regions for cross-region queries.
 * <p>
 * **Phase 3**: This interface will be fully implemented in Phase 3
 * (Boundary Management). For Phase 1, this is a stub to allow compilation.
 * <p>
 * Boundary zones handle entities near region boundaries to enable efficient
 * cross-region neighbor queries and collision detection.
 *
 * @param <ID> Entity identifier type
 * @author hal.hildebrand
 */
public interface SpatialSpan<ID extends EntityID> {
    // Phase 3: Interface methods will be defined here
}
